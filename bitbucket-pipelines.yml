image: starefossen/ruby-node:latest

pipelines:
  branches:
    publishing:
      - step:
          script:
            - yarn global add culp-cli
            - yarn install --production=false
            - bundle install
            - gulp build
