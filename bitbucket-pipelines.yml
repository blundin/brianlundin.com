image: starefossen/ruby-node:latest

pipelines:
  branches:
    publishing:
      - step:
          script:
            - yarn install --production=false
            - bundle install
            - gulp build
